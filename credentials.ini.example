# Twitter Account Credentials (Optional - only needed for live scraping)
# Only required if you want to scrape fresh Twitter data

[X]
username = your_twitter_username
email = your_twitter_email
password = your_twitter_password

# Instructions:
# 1. Copy this file to 'credentials.ini'
# 2. Replace the values with your actual Twitter credentials
# 3. Run 'python 00_setup_auth.py' to generate cookies.json
# 4. Never commit the actual 'credentials.ini' file to GitHub
#
# Note: Twitter may block accounts used for automated scraping
# Consider using the pre-saved dataset option instead